package cucumber.msdc.stepdefs;
/*
import junit.framework.Assert;
import cucumber.annotation.After;
import cucumber.annotation.Before;
import cucumber.annotation.en.Then;
import cucumber.annotation.en.When;
import cucumber.msdc.pages.models.HomePage;
import cucumber.runtime.PendingException;
*/

public class CompetitionsSteps extends AbstractSteps {
	/*

	@Before
	public void setUp() {
		intSetUp();
	}

	@After
	public void tearDown() {
		intTearDown();
	}
	
	
	@Then("^I should see competition \"([^\"]*)\" as default$")
	public void I_should_see_competition_as_default(String competition) throws Throwable {
	   Assert.assertEquals(competition, cukesApp.getHomePage().getSelectedCompetition());
	}


	@When("^I change competition to \"([^\"]*)\"$")
	public void I_change_competition_to(String comp) throws Throwable {
	    cukesApp.getHomePage().selectCompetition(comp);
	}

	@Then("^the match date should be different from \"([^\"]*)\"$")
	public void the_match_date_should_be_different_from(String date) throws Throwable {
		Assert.assertFalse(date.equals(cukesApp.getHomePage().getMatchDayDate()));
	}
*/

}
